---
disable-model-invocation: true
name: factory-apps
description: |
  Compatibility trigger for Canary, Landmark, and Mint routing, plus the work ledger.
  Loads /foundation factory surfaces. Prefer /foundation.
  Trigger: /factory-apps, /factory-stack.
argument-hint: "[canary|landmark|mint|gh]"
---

# /factory-apps

Compatibility entry only. Surfaces: `misty-canary`, `misty-landmark`, Mint, and the GitHub work ledger.

1. Load `/foundation`.
2. Answer from the **Factory surfaces** section.
3. For `canary|landmark|mint|gh` args, skip the full floor scorecard.
