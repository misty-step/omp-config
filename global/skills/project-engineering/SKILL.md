---
disable-model-invocation: true
name: project-engineering
description: |
  Retired. Use /foundation for the every-project floor (full audit and
  remediate). Trigger retained so old prompts still resolve.
argument-hint: "[repo-path]"
---

# /project-engineering → /foundation

This skill is a **redirect**.

Run **`/foundation`** on the target repo. That skill always scores and remediates
the full every-project floor (application floor, faces, gates, tests, release,
onboarding, Factory). It has no adopt/audit modes.

Load: `global/skills/foundation/SKILL.md`.
