# Purpose: Tier-1 core only — routing, diagnosis, gates, credentials. Drops managed and project skills.
# Launch: omp --config ~/.omp/agent/presets/lean.yml
# Filter: allowlist. Since 2026-07-21 the main library is tiered; this preset equals the visible core.
skills:
  includeSkills:
    - next
    - research
    - code-review
    - shape
    - diagnose
    - ci
    - mint
