# /groom eval

The durable null-case result for `/groom` is the 2026-07-21 Crucible blind
A/B sweep. Arm A loaded `/groom`; arm B received the same live backlog task
with a sharp operator prompt and no skill. The skill won both paired fixtures.

Evidence: `~/Development/crucible/runs/local/skill-null-sweep/report.md`.

## Verdict

**Keep.** The skill forced live card relations, stale-claim state, PR/git
readback, and explicit close/consolidate decisions that the bare arm skipped.

## Sample limits

Two fixtures; one worker trial per arm per fixture; one Grok grader family; and
an unanchored proxy result. The result is evidence for this bounded sweep, not a
population estimate. Re-run after a contract or major-model change.
