# OpenRouter uses the official upstream URL. Agent Vault's service rule owns
# and replaces Authorization; this is a non-secret client-side sentinel.
providers:
  openrouter:
    baseUrl: "https://openrouter.ai/api/v1"
    apiKey: "__OPENROUTER_API_KEY__"
