# Local Ollama discovery. Cloud providers stay in the bundled catalog.
providers:
  ollama:
    baseUrl: "http://127.0.0.1:11434"
    auth: none
    api: openai-responses
    discovery:
      type: ollama
